language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t sergiohpreis/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true sergiohpreis/docker-react npm run test